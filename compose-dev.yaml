services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: swift:amazonlinux2-slim
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

